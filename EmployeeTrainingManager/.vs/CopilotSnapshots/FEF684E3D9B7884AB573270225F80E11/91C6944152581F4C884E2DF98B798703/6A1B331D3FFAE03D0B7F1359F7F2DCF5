using System.Globalization;
using System.Windows.Data;
using EmployeeTrainingManager.Domain.Entities;

namespace EmployeeTrainingManager.Presentation.Converters
{
    public class TrainingInfoConverter : IValueConverter
    {
        public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
        {
            if (value is Training training)
            {
                return training.ShowInfo();
            }
            return string.Empty;
        }

        public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
        {
            throw new NotImplementedException();
        }
    }
}
